################### Cassandrabeat Configuration Example #########################

############################# Cassandrabeat ######################################

cassandrabeat:
  # Defines how often an event is sent to the output
  period: 10s

  # The table(s) for which to get statistics
  table:
    - "system.local"
    - "system.peers"
